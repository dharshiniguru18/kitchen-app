apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: kitchen-app
spec:
  type: NodePort   #  CHANGE THIS
  selector:
    app: api
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30081   #  ADD THIS